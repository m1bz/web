{
  "name": "spartacus-fitness",
  "version": "1.0.0",  
  "scripts": {
    "setup-db": "node scripts/setup-database.js",
    "start": "npm run setup-db && node server.js",
    "dev": "npm run setup-db && node server.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "multer": "^2.0.1",
    "pg": "^8.16.0"
  }
}
